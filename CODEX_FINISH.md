作業終了時だけ読む。

確認するもの:

* ユーザー依頼に対応した差分だけか
* ソース変更に対応する `docs/<source path>.md` を更新したか
* 全体構成が変わったら `SOURCE_FILES.md` を更新したか
* 目的、優先順位、MVP、非目標が変わったら `GOALS.md` を更新したか
* OAuth、scope、callback、X API確認手順が変わったら `REAL_API_VERIFICATION.md` を更新したか
* build、test、lint、実機検証、安全スクリプトが変わったら `SAFE_DEBUG_ROUTINE.md` を更新したか
* 実機統合テスト要件や証跡が変わったら `TEST_REQUIREMENTS_COVERAGE.md` を更新したか
* 実行した検証、未確認事項、実機確認の有無を簡潔に報告したか
* コミットはユーザーが求めた場合だけ行う
