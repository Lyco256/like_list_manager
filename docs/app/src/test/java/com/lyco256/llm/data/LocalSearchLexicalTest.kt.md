# LocalSearchLexicalTest

人工文字列によるliteral FTS生成、引用符escape、operator安全性、重複query除去、3 code points未満trigram除外、token multiplicity、Unicode OSA転置と長さ上限、anagram signature・割当・同一token除外、固定score分岐・source整合性のテスト。自然言語の期待順位はassertしない。
