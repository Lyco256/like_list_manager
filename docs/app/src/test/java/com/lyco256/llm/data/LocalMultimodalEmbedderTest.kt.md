# `LocalMultimodalEmbedderTest.kt`

`MultimodalEmbedding`の256次元・finite・正規化制約、token入力のCLS／mask／position、Japanese CLIP 3資産のmetadata／SHA検証、再利用、破損資産の部分復旧、`.partial` cleanup、コピー途中失敗をJVM unit testで確認します。類似度や検索品質は判定しません。
