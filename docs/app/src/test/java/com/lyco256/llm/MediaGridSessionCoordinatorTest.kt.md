# `MediaGridSessionCoordinatorTest.kt`

session keyが列数、source revision、hierarchy revisionではなく有効なfilter/sortだけで決まること、明示的なfilter/sort変更とduplicate mode/generationだけが別session identityになること、スクロールバー完了checkpointとscroll終了checkpointが二重化しないことを検証します。
