
# `OcrUi.kt`

UI helpers for the tweet options menu and the OCR dialog.

## Responsibilities

- Renders the three-dot tweet options menu and exposes the OCR and local-delete actions with test tags, including per-clip button tags supplied by the caller.
- Shows the OCR dialog with image previews, a horizontal pager for multiple images, progress state, error text, and confirm/cancel buttons.
